void setup(){
    //define o pino e o tipo: entrada ou saida
    //pinMode(10, OUTPUT);
}

void loop(){

    // liga o led    no pino 10 a cada 500 milisegundos
    // digitalWrite (10, HIGH);
    // delay(500);

     // desliga o led no pino 10 a cada 500 milisegundos
    // digitalWrite (10, LOW);
    // delay(500);
}